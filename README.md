copy xmonad config to xmonad directory
```bash
sudo cp xmonad.hs ~/.xmonad/
```
copy xmobar config to xmobar directory
```bash
sudo cp xmobarrc ~/.config/xmobar/
```
dunst config 
```bash
sudo cp dunstrc ~/.config/dunst/
```
